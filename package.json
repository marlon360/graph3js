{
  "name": "graph3js",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "parcel dev index.html --host=141.22.69.73",
    "build": "npm run clean && parcel build index.html --no-source-maps -d public",
    "clean": "rimraf ./public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "rimraf": "^3.0.0",
    "typescript": "^3.7.2"
  },
  "dependencies": {
    "expr-eval": "^2.0.2",
    "three": "^0.111.0",
    "three-orbitcontrols": "^2.110.1",
    "three-vrcontroller-module": "^1.0.5"
  }
}
